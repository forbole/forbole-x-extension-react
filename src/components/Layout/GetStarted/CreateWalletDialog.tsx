import React from 'react'

interface Props {
    open: boolean;
    onClose: (open: boolean) => void;
}

const CreateWalletDialog = (props: Props) => {
    return (
        <div>
            
        </div>
    )
}

export default CreateWalletDialog
